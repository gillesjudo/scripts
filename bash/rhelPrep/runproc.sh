#Create a function to check if a given process is running. 
#The function should take the process name as an argument and return 0 if the process is running and 1 if it is not. Use the pgrep command to check if the process is running.
